<style type="text/css">
/* Add custom styles to your entire online store */
	.btn {
		border-radius: 4px;
		font-weight: 600;
		letter-spacing: 0.5px;
	}
	sht-menu-drwer.drawer {
		display: flex !important;
	}
	.bwp-badge {
		padding: 0.75rem;
		pointer-events: none;
	}
	.bwp-badge img {
		width: 5rem;
		height: 5rem;
		transform: none !important;
	}
	.badge {
		font-size: clamp(10px, 1.35vw, 14px);
	}
	.menu-dropdown .menu__link[aria-label="SALE"] {
		color: var(--ts-color-red);
		font-weight: 700;
	}
	.footer-newsletter--embed-type .needsclick[data-testid=form-row] p * {
		color: var(--color-normal-text) !important;
	}

	@media only screen and (min-width: 768px) {
		.bwp-badge {
			padding: 1rem;
		}
		.bwp-badge img {
			width: 10rem;
			height: 10rem;
		}
	}

	@media only screen and (max-width: 991px) {
		.template-collection .products [style*="--w-md"] {
			--w-md: 4 !important;
			--gutter: 10px;
		}
	}

	html.mbau-wholesale div#bik-chat-root {
		display: none !important;
	}
</style>