<style type="text/css">
/* Add custom styles to your entire online store */
	.btn {
		border-radius: 4px;
		font-weight: 600;
		letter-spacing: 0.5px;
	}
	sht-menu-drwer.drawer {
		display: flex !important;
	}
	.bwp-badge {
		padding: 0.75rem;
		pointer-events: none;
	}
	.bwp-badge img {
		width: 5rem;
		height: 5rem;
		transform: none !important;
	}
	.badge {
		text-transform: uppercase;
		font-size: clamp(10px, 1.35vw, 14px);
	}
	.badge.mbp-badge {
		background-color: var(--ts-color-putty);
	}
	.badge.mbr-badge {
		background-color: var(--ts-color-red);
	}
	.badge.mbb-badge {
		background-color: var(--ts-color-black);
	}
	.product__media-wrapper .badge {
		position: absolute;
		top: 0;
		right: 0;
		z-index: 1;
	}
	.menu-dropdown .menu__link[aria-label="SALE"] {
		color: var(--ts-color-red);
		font-weight: 700;
	}
	.footer-newsletter--embed-type .needsclick[data-testid=form-row] p * {
		color: var(--color-normal-text) !important;
	}
	.product.product-card .price .price__sale > .price-item {
		color: var(--ts-color-red);
		font-weight: 700;
	}
	.product.product-card .price .price__sale .price-item--regular {
		color: rgba(var(--color-normal-text-rgb));
	}

	@media only screen and (min-width: 768px) {
		.bwp-badge {
			padding: 1rem;
		}
		.bwp-badge img {
			width: 10rem;
			height: 10rem;
		}
	}

	@media only screen and (max-width: 991px) {
		.template-collection .products [style*="--w-md"] {
			--w-md: 4 !important;
			--gutter: 10px;
		}
	}

	html.mbau-wholesale div#bik-chat-root {
		display: none !important;
	}
</style>